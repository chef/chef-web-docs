Chef Automate has the following operating system requirements:

- A Linux operating system with kernel version 3.2 or later
- `systemd` as the init system
- The `useradd` command-line utility for creating user accounts
- Either `curl` or `wget` for downloading installation assets
