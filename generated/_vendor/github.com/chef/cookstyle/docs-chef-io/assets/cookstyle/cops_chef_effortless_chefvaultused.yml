---
short_name: ChefVaultUsed
full_name: Chef/Effortless/ChefVaultUsed
department: Chef/Effortless
description: Chef Vault is not supported in the Effortless pattern, so usage of Chef
  Vault must be shifted to another secrets management solution before leveraging the
  Effortless pattern.
autocorrection: false
target_chef_version: All Versions
examples: |2-

  # bad
  require 'chef-vault'

  # bad
  ChefVault::Item

  # bad
  include_recipe 'chef-vault'

  # bad
  chef_gem 'chef-vault'

  # bad
  chef_vault_item_for_environment(arg, arg1)

  # bad
  chef_vault_item(arg, arg1)
version_added: '6.19'
enabled: false
excluded_file_paths:
- "**/metadata.rb"
- "**/Berksfile"
