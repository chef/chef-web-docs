steps:

  - label: Update cookstyle cops YAML and MD files
    command:
      - .expeditor/buildkite/update_hugo_modules_cookstyle.sh
    timeout_in_minutes: 10
    retry:
      automatic:
        limit: 1
    expeditor:
      executor:
        docker:
